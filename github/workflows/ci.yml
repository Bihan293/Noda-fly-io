name: Noda CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        go-version: ['1.22']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Verify Go version
        run: go version

      - name: Build
        run: go build ./...

      - name: Run tests with race detector
        run: go test ./... -v -race -count=1

      - name: Static analysis (go vet)
        run: go vet ./...

      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "The following files are not properly formatted:"
            echo "$unformatted"
            exit 1
          fi

      - name: Fuzz tests (block serializer/parser)
        run: |
          go test -fuzz=FuzzSerializeTxForHash -fuzztime=15s ./block/
          go test -fuzz=FuzzMerkleRoot -fuzztime=10s ./block/
          go test -fuzz=FuzzBlockHeaderSerialization -fuzztime=10s ./block/

      - name: Fuzz tests (P2P message framing)
        run: |
          go test -fuzz=FuzzP2PMessageRoundTrip -fuzztime=10s ./p2p/
          go test -fuzz=FuzzP2PReadMessageMalformed -fuzztime=10s ./p2p/

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t noda:test .

      - name: Test Docker image
        run: |
          docker run -d --name noda-test -p 3000:3000 noda:test
          sleep 5
          curl -sf http://localhost:3000/health || exit 1
          docker stop noda-test
          docker rm noda-test
